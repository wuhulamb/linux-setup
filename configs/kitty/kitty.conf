include ./theme.conf

# 默认字号 12.0；AMD Ryzen 7 7840 设备由 apply.sh 自动改为 14.0（可用 KITTY_FONT_SIZE 覆盖）
font_size 12.0

# 重置当前字号为初始值（kitty_mod 默认 Ctrl+Shift）
map kitty_mod+0 change_font_size all 0
# 滚动到顶部 / 底部
map kitty_mod+; scroll_home
map kitty_mod+' scroll_end