# 验证产物 / 构建产物（不进 git）
artifacts/
work/
logs/
*.log
*.iso
*.img
*.qcow2
*.fd
*.ppm

# 私密信息
secrets/*
!secrets/README.md
*.secret
*credentials*
.env

# 真实网络代理配置（仅运行期使用；模板见 configs/proxy/*.example）
/shadowsocks-config.json
configs/proxy/*.json

# 仅保留用到的 kitty 主题；其余合集忽略
configs/kitty/kitty-themes/

# 编辑器
__pycache__/
*.swp
configs/fonts/files/
